# 发票检查器 - 部署指南

## 📋 当前状态

✅ 项目结构已创建  
✅ 后端API代码完成  
✅ 前端界面完成  
⏳ 依赖安装中...  

## 🏗️ 项目结构

```
invoice-checker/
├── backend/              # FastAPI 后端
│   ├── main.py          # 主应用 (API路由)
│   ├── requirements.txt # Python依赖
│   └── invoice_checker.db  # SQLite数据库(自动创建)
├── frontend/            # Next.js 前端
│   ├── app/
│   │   ├── page.tsx    # 主页面(拖拽上传+合同列表)
│   │   ├── layout.tsx  # 布局
│   │   └── globals.css # 全局样式
│   ├── package.json    # Node依赖
│   └── tailwind.config.ts
├── start-dev.sh        # 本地开发启动脚本
└── README.md
```

## 🚀 本地测试步骤

### 方法1: 使用启动脚本(推荐)

```bash
cd tools/invoice-checker
chmod +x start-dev.sh
./start-dev.sh
```

### 方法2: 手动启动

**后端:**
```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --reload --port 8000
```

**前端(新终端):**
```bash
cd frontend
npm install
npm run dev
```

访问: http://localhost:3000

## 🌐 部署到生产环境

### 选项1: Vercel (前端) + Railway (后端)

**前端部署 (Vercel - 免费):**
1. 推送代码到GitHub
2. 在 Vercel 导入项目
3. 根目录设置为 `frontend/`
4. 添加环境变量: `NEXT_PUBLIC_API_URL=<后端API地址>`
5. 部署

**后端部署 (Railway - 免费额度):**
1. 在 Railway 创建新项目
2. 连接GitHub仓库
3. 根目录设置为 `backend/`
4. 添加启动命令: `uvicorn main:app --host 0.0.0.0 --port $PORT`
5. 部署

### 选项2: 一体化部署 (Render / Fly.io)

创建 `Dockerfile`:
```dockerfile
# 多阶段构建
FROM node:18 AS frontend-build
WORKDIR /app/frontend
COPY frontend/package*.json ./
RUN npm install
COPY frontend/ ./
RUN npm run build

FROM python:3.11
WORKDIR /app
COPY backend/requirements.txt ./
RUN pip install -r requirements.txt
COPY backend/ ./
COPY --from=frontend-build /app/frontend/out ./static

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

## 📝 MVP功能清单

当前已实现:
- ✅ 文件拖拽上传界面
- ✅ 合同/发票切换上传
- ✅ 合同数据存储(SQLite)
- ✅ 发票关联验证
- ✅ 金额对比和状态标记(绿/黄)
- ✅ 合同列表展示(按日期排序)
- ✅ 发票明细展开/折叠

待实现(第二阶段):
- ⏳ 真实OCR识别(目前用mock数据)
- ⏳ 规格型号模糊匹配
- ⏳ 领星API集成
- ⏳ 飞书机器人集成
- ⏳ 微信文件桥接

## 🧪 测试步骤

1. **启动应用**
2. **上传合同**: 切换到"上传合同"，拖入PDF
3. **查看列表**: 应该出现黄色标记(未完成)
4. **上传发票**: 切换到"上传发票"，拖入PDF
5. **验证结果**: 
   - 如金额匹配 → 绿色标记
   - 如金额不足 → 黄色标记显示欠款

## 🔧 下一步

帆帆，目前依赖正在安装中。安装完成后，你可以:

1. **本地测试**: 运行 `./start-dev.sh`
2. **确认功能**: 测试上传和验证流程
3. **部署**: 我可以帮你部署到Vercel + Railway (免费)

需要我现在就开始部署到线上环境吗？还是先在本地测试一下？💪
