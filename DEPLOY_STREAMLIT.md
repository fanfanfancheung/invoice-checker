# 发票检查器 - Streamlit Cloud 部署指南

## 🚀 5分钟部署到线上

### 📋 准备工作（已完成✓）

- ✅ 代码已推送到GitHub: https://github.com/fanfanfancheung/invoice-checker
- ✅ `streamlit_app.py` 已准备好
- ✅ `requirements.txt` 依赖文件已配置

---

## 🎯 部署步骤（超简单！）

### 步骤1: 注册/登录 Streamlit Cloud

访问: https://share.streamlit.io

- 用GitHub账号直接登录（推荐）
- 或者用Google/Email注册

### 步骤2: 创建新应用

1. 点击右上角 **"New app"** 按钮

2. 填写三个参数:

   ```
   Repository:   fanfanfancheung/invoice-checker
   Branch:       master
   Main file:    streamlit_app.py
   ```

3. （可选）自定义App URL:
   ```
   App URL: invoice-checker (自定义名称)
   ```

4. 点击 **"Deploy!"** 按钮

### 步骤3: 等待部署完成

- ⏱️ 大约需要 1-2 分钟
- 🎬 可以看到实时部署日志
- ✅ 看到 "Your app is live!" 就成功了

### 步骤4: 获取在线地址

部署成功后会得到类似这样的地址:

```
https://<your-app-name>.streamlit.app
```

或者

```
https://share.streamlit.io/<username>/<repo-name>/<branch>/streamlit_app.py
```

---

## 📸 部署截图指南

### 1. 登录页面
```
┌────────────────────────────────┐
│  Streamlit                     │
│                                │
│  [Continue with GitHub] ← 点这个│
│  [Continue with Google]        │
│  [Continue with Email]         │
└────────────────────────────────┘
```

### 2. 新建应用页面
```
┌────────────────────────────────────────┐
│  New app                               │
│                                        │
│  Repository *                          │
│  [fanfanfancheung/invoice-checker]    │
│                                        │
│  Branch *                              │
│  [master]                              │
│                                        │
│  Main file path *                      │
│  [streamlit_app.py]                    │
│                                        │
│  App URL (optional)                    │
│  [invoice-checker]                     │
│                                        │
│            [Deploy!]                   │
└────────────────────────────────────────┘
```

### 3. 部署中...
```
┌────────────────────────────────────────┐
│  Deploying your app...                 │
│                                        │
│  ⏳ Building Docker image...           │
│  ⏳ Installing dependencies...         │
│  ⏳ Starting app...                    │
│                                        │
│  [View logs ▼]                         │
└────────────────────────────────────────┘
```

### 4. 部署成功！
```
┌────────────────────────────────────────┐
│  🎉 Your app is live!                  │
│                                        │
│  https://invoice-checker.streamlit.app │
│                                        │
│  [Share] [Settings] [Reboot] [Delete] │
└────────────────────────────────────────┘
```

---

## 🔧 常见问题

### Q1: 找不到 "New app" 按钮？

**A:** 首次登录需要先连接GitHub账号：
1. 点击右上角头像
2. Settings → Connected accounts
3. Connect GitHub

### Q2: 部署失败，显示 "ModuleNotFoundError"？

**A:** 检查 `requirements.txt` 是否在根目录，内容为:
```
streamlit==1.31.0
pandas==2.2.0
```

### Q3: 应用部署成功但打不开？

**A:** 等待1-2分钟，Streamlit Cloud需要时间启动容器。刷新页面即可。

### Q4: 数据会丢失吗？

**A:** 
- ⚠️ Streamlit Cloud免费版：应用休眠后数据库会重置
- ✅ 解决方案：
  1. 连接外部数据库（PostgreSQL）
  2. 使用Streamlit的 `st.secrets` 存储连接信息
  3. 定期导出备份

### Q5: 如何更新应用？

**A:** 
1. 修改代码后推送到GitHub
2. Streamlit Cloud会自动检测并重新部署
3. 或者在应用页面点击 "Reboot" 手动重启

### Q6: 能处理多少用户？

**A:** 
- 免费版：适合小团队（<10人同时使用）
- 企业版：无限制

---

## 🎨 自定义配置（可选）

### 修改主题颜色

编辑 `.streamlit/config.toml`:

```toml
[theme]
primaryColor = "#3B82F6"        # 主色调
backgroundColor = "#F9FAFB"      # 背景色
secondaryBackgroundColor = "#FFFFFF"
textColor = "#1F2937"
font = "sans serif"
```

推送到GitHub后会自动应用。

### 添加密码保护

在 `streamlit_app.py` 开头添加:

```python
import streamlit as st

def check_password():
    if st.session_state.get("password_correct", False):
        return True
    
    pwd = st.text_input("密码", type="password")
    if st.button("登录"):
        if pwd == "your_password":
            st.session_state.password_correct = True
            st.rerun()
        else:
            st.error("密码错误")
    return False

if not check_password():
    st.stop()
```

---

## 📊 使用限制（免费版）

| 项目 | 限制 |
|------|------|
| **应用数量** | 无限 |
| **流量** | 无限 |
| **CPU** | 共享 |
| **内存** | 1 GB |
| **存储** | 临时（应用休眠后丢失） |
| **休眠时间** | 7天无访问自动休眠 |
| **唤醒时间** | 约30秒 |

**升级企业版:**
- 专用资源
- 持久化存储
- 自定义域名
- 团队协作

---

## 🚀 部署后优化

### 1. 连接PostgreSQL数据库

```python
# streamlit_app.py
import streamlit as st
import psycopg2

# Streamlit secrets管理
db_url = st.secrets["DATABASE_URL"]
conn = psycopg2.connect(db_url)
```

在Streamlit Cloud添加 secrets:
1. App设置 → Secrets
2. 粘贴:
   ```toml
   DATABASE_URL = "postgresql://user:pass@host:5432/db"
   ```

### 2. 添加缓存提升性能

```python
@st.cache_data(ttl=600)  # 缓存10分钟
def get_all_contracts():
    # 数据库查询
    pass
```

### 3. 配置自动重启

在仓库根目录添加 `.streamlit/secrets.toml`:

```toml
[general]
email = "your-email@example.com"
```

---

## 📱 分享应用

部署成功后，可以通过以下方式分享:

1. **直接链接**
   ```
   https://your-app.streamlit.app
   ```

2. **嵌入iframe**
   ```html
   <iframe src="https://your-app.streamlit.app" width="100%" height="600"></iframe>
   ```

3. **二维码**
   - 在应用页面点击 "Share"
   - 生成二维码分享

---

## ✅ 检查清单

部署前确认:

- [ ] GitHub仓库可访问
- [ ] `streamlit_app.py` 在根目录
- [ ] `requirements.txt` 包含所有依赖
- [ ] 没有敏感信息硬编码（用 secrets 管理）
- [ ] 本地测试通过 (`streamlit run streamlit_app.py`)

---

## 🎉 完成！

部署成功后，你就拥有一个在线的发票检查器应用了！

**下一步:**
1. 测试功能是否正常
2. 分享链接给团队成员
3. 根据反馈迭代优化

需要帮助？查看 [Streamlit文档](https://docs.streamlit.io) 或者问我！💪
